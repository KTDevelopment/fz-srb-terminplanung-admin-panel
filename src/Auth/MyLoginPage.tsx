import { Login } from "react-admin";
import { LoginProps } from "ra-ui-materialui/src/auth/Login";

export const LoginPage = (props: LoginProps) => (
  <Login {...props} backgroundImage="" />
);

// export default MyLoginPage as LoginComponent;
